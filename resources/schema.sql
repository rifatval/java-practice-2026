create table account (
                         id serial primary key,
                         name varchar(20),
                         email varchar(20),
                         password varchar(20),
                         profile_description varchar(100)
);